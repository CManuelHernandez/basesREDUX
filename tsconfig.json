{
  "compilerOptions": {
    "target": "ES6", // Define la versión de ECMAScript.
    "module": "CommonJS", // Usa CommonJS para la compatibilidad con Node.js.
    "esModuleInterop": true, // Permite la interoperabilidad con ES6 y CommonJS.
    "strict": true, // Habilita estrictas comprobaciones de tipos.
    "outDir": "./dist" // Carpeta de salida para los archivos compilados.
  },
  "include": ["**/*.ts"], // Incluye todos los archivos .ts en el proyecto.
  "exclude": ["node_modules"] // Excluye la carpeta node_modules.
}
